<% @cart.items.each do |item| %>
  <%= item.title %>
  <%= item.description %>
  <%= item.price %>
  <%= item.image %>
  <%= link_to "Remove", cart_path(item_id: item.id), method: :delete %>
<% end %>